# Example env for backend (safe to commit)
# Copy this to backend/.env and fill the real secret values before running.

# Database Configuration
DB_NAME=todo_db
DB_USERNAME=todo_user
DB_PASSWORD=your_secure_password_here
DB_PORT=5432
DB_HOST=localhost

# Optionally override the JDBC URL if you created a different DB name
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/todo_app
# SERVER_PORT=8080

# JWT / App settings (optional)
# APP_SECRET=replace_with_a_secure_secret
# TOKEN_EXPIRY_SECONDS=3600

# If you run the frontend separately, set NEXT_PUBLIC_API_URL there, e.g.:
# NEXT_PUBLIC_API_URL=http://localhost:8080
